{% extends 'base.html.twig' %}
{% block content %}
    <div class="container rounded bg-white mt-5 mb-5 profile">
        <div class="row">
            <div class="col-md-9">
                <div class="p-3 py-5">
                    <div class="d-flex justify-content-center align-items-center mb-3">
                        <h1 class="text-right">Contact Us</h1>
                    </div>
                    <div class="row mt-2">
                        <div class="col"><label class="labels">Address</label><input type="text" class="form-control" value="{{ address }}" readonly></div>
                        </div>
                    <div class="row mt-3">
                        <div class="col-md-12 my-2"><label class="labels">Mobile Number</label><input type="text" class="form-control"  value="{{ mobile }}" readonly></div>
                        <div class="col-md-12 my-2"><label class="labels">Email ID</label><input type="text" class="form-control" value="{{ mobile }}" readonly></div>
                    </div>
                </div>
            </div>

        </div>
    </div>
{% endblock %}